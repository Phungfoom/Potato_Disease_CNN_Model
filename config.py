# Data dimensions
DATA_PARAMS = {
    "image_size": (224, 224),
    "batch_size": 32,
    "seed": 123}

NUM_CLASSES = 3  # Need to change later, just a good base
DROPOUT_RATE = 0.3
EPOCHS = 10

# Paths
BASE_DIR = 'hot_potato'
OUTPUT_DIR = 'outputs'